[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sb_calibration"
version = "0.0.0"
description = "Refactored SB calibration package (workspace shim)"
readme = "README.md"
requires-python = ">=3.8"
